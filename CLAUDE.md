# CLAUDE.md — контекст проекта «Аппарат»

Это файл-память для Claude Code. Читай его перед работой над проектом.

## Что это
2D-аркада на **Phaser 3** + **Vite**. Игрок — Серёга (руководитель Аппарата) —
ловит поручения, которые кидает Карен (генеральный директор), и сгружает их
в лоток нужного департамента по цвету. Тема — сатира на аппаратную работу.

## Как запустить
```bash
npm install
npm run dev      # запуск dev-сервера с горячей перезагрузкой (порт 5173)
npm run build    # сборка в dist/
npm run preview  # предпросмотр сборки
```

## Структура
```
index.html              — точка входа Vite
src/
  main.js               — конфигурация Phaser, список сцен
  core/
    config.js           — ВСЕ константы: размеры, палитра PAL, департаменты DEPTS, баланс BALANCE
    office.js           — процедурный фон-кабинет (окно, город, кран, свет)
  scenes/
    BootScene.js        — загрузка спрайтов + генерация текстур (папки, частицы, виньетка)
    MenuScene.js        — стартовый экран
    GameScene.js        — основной игровой цикл (главный файл логики)
    OverScene.js        — экран окончания
public/
  sprites/              — PNG персонажей (karen_*, sergey_*)
```

## Где что менять
- **Баланс сложности** (скорость, темп, жизни, руки) — только `src/core/config.js`, объект `BALANCE`.
- **Цвета и палитра** — `src/core/config.js`, объекты `PAL` и `DEPTS`.
- **Игровая логика** (ловля, маршрутизация, настроение) — `src/scenes/GameScene.js`.
- **Фон/атмосфера** — `src/core/office.js`.

## Персонажи (спрайты)
- `karen_idle / karen_throw / karen_angry` — генеральный: покой, бросок, злой бросок.
- `sergey_idle / sergey_catch / sergey_carry` — Аппарат: покой, ловит (руки раскрыты), несёт папки.
- Спрайты сгенерированы во внешнем генераторе по фото реальных людей (дружеский шарж),
  фон снят программно. При замене — класть PNG с прозрачным фоном в `public/sprites/`.

## Текущее состояние
- Реализовано: ловля, 3 департамента, цветные папки, серые поручения, таймеры,
  настроение босса с реакцией мира (краснеет), комбо, частицы, тряска экрана.
- Department-лотки перегружаются при 4 сгрузках подряд (блок на ~4 сек).
- **Звук** (`src/core/audio.js`) — синтез Web Audio без файлов; выключатель M.
- **Реплики Карена** — облачко при броске (`GameScene.karenSay`).
- **Бонус-волна «Конец квартала»** — каждые 150 очков, ×2 и аврал.
- **Прогрессия раундами** (`src/core/run.js`, `src/scenes/ShopScene.js`):
  раунд на время (30с, +15с/раунд, до 90с), сложнее с каждым раундом.
  В конце — премия (очки + бонус за жизни) в бюджет (₽). Между раундами —
  магазин улучшений (накопительные): отпуск/скорость, помощница Оля
  (авто-ловец, `GameScene.spawnOlya/updateOlya`), портфель, жизни, ×коэффициент,
  кофемашина. Кончились жизни — карьера всё (Over: дошёл до раунда N).
  Рекорд карьеры — самый далёкий раунд (localStorage `apparat_best_round`).
- **Мобильная версия:** адаптивный масштаб (`Phaser.Scale.FIT` в `main.js`),
  сенсорное управление в `GameScene.setupTouch()` — вести пальцем + тап по лотку.
  В портретной ориентации `index.html` показывает «поверни телефон».
- Опубликовано на GitHub Pages, авто-деплой через `.github/workflows/deploy.yml`.

## Режим «Аппаратная дуэль» (файтинг)
- **Бойцы — data-driven.** Единый источник правды: `src/core/fighters.js`
  (объект `FIGHTERS`). Новый боец = новая запись + спрайты
  `public/sprites/fighters/<key>_<pose>.png` (позы: idle, light, heavy,
  special, hit, block, win, ko). Поля: `stats` (maxHealth, attackPower,
  defense, speed, weight), `moves.light/heavy`, `archetype`, `aiProfile`,
  `nativeRight` (в какую сторону смотрит исходный спрайт), `available`,
  `uniqueMechanic`. Игрок — `seryoga` (playable). `OPPONENTS` = все не-playable.
- **Баланс файтинга** — `src/core/balance.js`: `FIGHT` (урон ульты, блок,
  hitstop, тряска, метр) и `AI_PROFILES` (поведение ИИ по архетипу).
- **ИИ по архетипу** — `BattleScene.ai()` читает `AI_PROFILES[cfg.aiProfile]`:
  boss прёт и бьёт тяжело, defensive держит дистанцию и блокирует,
  counter ставит блок и наказывает (`punish`), aggressive рашит.
  Профиль задаёт range/approach/aggression/heavy/block/reactBlock/retreat/cool.
- **Логика боя** — `src/scenes/BattleScene.js`: класс `Fighter` собирается из
  cfg; ходьба, лёгкий/тяжёлый удар, блок, супер-метр + ульта «На проверку ДЭБ»,
  KO с добиванием. Арены — `ARENAS` в config.js, фоны `public/bg/<key>.jpg`.
- **Экраны** — `OpponentSelectScene` (выбор соперника, лестница карьеры),
  `ArenaSelectScene` (выбор арены). Заход: меню → «В ЗАРУБУ».

## Заметки
- **Спрайт Оли — плейсхолдер** (тонированный розовым `sergey_catch`). При
  появлении настоящего арта — класть `public/sprites/olya_*.png` и заменить
  текстуры/тинт в `GameScene.spawnOlya/olyaCatch/olyaDrop`.
- Баланс раундов/магазина — `src/core/run.js` (длительность, сложность, цены,
  эффекты улучшений в `effStats`).

## Идеи на будущее (бэклог)
- Рисованный фон-кабинет вместо процедурного (генератор изображений → public/).
- Звук и музыка (Web Audio / библиотека бесплатных сэмплов).
- Реплики Карена всплывающим текстом при бросках.
- Бонус-волна «конец квартала».
- Таблица рекордов.

## Стиль кода
- ES-модули, без сборщиков кроме Vite.
- Константы — в config.js, не хардкодить в сценах.
- Комментарии по-русски, кратко и по делу.
